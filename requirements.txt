# Pinned to the versions the reported results were produced with.
# Python 3.10+
pandas==2.3.3
numpy==2.2.6
scipy==1.15.3
scikit-learn==1.7.2
lightgbm==4.7.0
xgboost==3.2.0
shap==0.49.1
matplotlib==3.10.9
seaborn==0.13.2
joblib==1.5.3
pyarrow>=15.0
streamlit==1.60.0
pytest>=8.0

# Ships the five NYC Flights 2013 tables so the project runs without a Kaggle
# account. Drop the Kaggle CSVs into data/raw/ to use those instead.
nycflights13==0.0.3
